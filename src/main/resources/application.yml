# 서버 포트 설정
server.port=8085
  
  # Spring Boot 데이터베이스 연동 설정
spring.datasource.url=jdbc:mariadb://localhost:3308/boarddb?allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=root1234
spring.datasource.driver-class-name=org.mariadb.jdbc.Driver
  
  # MyBatis 설정
mybatis.mapper-locations=classpath:mapper/*.xml
mybatis.type-aliases-package=com.example.board1.vo